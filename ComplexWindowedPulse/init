#!/bin/bash

# Usage: ./inspect matrix T W r m
# See `init.jl` for details on arguments.

PROJDIR="$HOME/HPCJobs/ComplexWindowedPulse"       # PRE-COMPILED JULIA PROJECT DIRECTORY
JULIASCRIPT="${PROJDIR}/init.jl"

julia --project="${PROJDIR}" "${JULIASCRIPT}" "$@"